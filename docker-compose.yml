services:
  proxy:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env  # copy .env.example to .env and fill in API keys before running
    volumes:
      - ./backend/data:/app/backend/data  # persist audit logs/task index across restarts
