pydantic>=2.0
pyyaml>=6.0
pytest>=8.0
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
httpx>=0.27
playwright>=1.40   # after installing, also run: python -m playwright install chromium
groq>=0.11
python-dotenv>=1.0

# Add once the team wires up the real reasoning loop (see agent/reasoner.py):
# anthropic
